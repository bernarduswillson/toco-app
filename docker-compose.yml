version: '3.3'
services:
  web:
    image: tubes-1:latest
    ports:
      - 8008:80
    volumes:
      - .:/var/www/html
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - UPLOAD_LIMIT=10M

